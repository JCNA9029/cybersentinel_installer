# CyberSentinel v1 — Python Dependencies
# Install: pip install -r requirements.txt

# ── Core ──────────────────────────────────────────────────────
requests>=2.31.0        # Cloud API calls (VirusTotal, OTX, MetaDefender, MalwareBazaar)
psutil>=5.9.0           # Live process enumeration and network monitoring
watchdog>=3.0.0         # Daemon filesystem observer
cryptography>=41.0.0    # Fernet AES-128 encryption for API keys
colorama>=0.4.6         # Terminal color output

# ── ML Engine ─────────────────────────────────────────────────
pefile>=2024.8.26       # Windows PE feature extraction
lightgbm>=4.1.0         # Tier 2 malware classifier
numpy>=1.24.0           # Feature array processing
scipy>=1.11.0           # Required by SHAP TreeExplainer
tqdm>=4.65.0            # Progress display during model operations
pandas>=2.0.0           # Evaluation harness result tabulation

# ── SHAP Explainability (Novel Contribution 2) ────────────────
shap>=0.44.0            # Real-time per-scan feature attribution

# ── AI Analyst (Tier 3) ───────────────────────────────────────
ollama>=0.1.0           # Local LLM interface (run: ollama pull qwen2.5:3b)

# ── SOC Dashboard ─────────────────────────────────────────────
flask>=3.0.0            # Web dashboard (dashboard.py)

# ── Desktop GUI ───────────────────────────────────────────────
PyQt6>=6.6.0            # Native desktop GUI (gui.py)

# ── Windows-Only (install separately) ────────────────────────
# pip install pywin32   — required for AMSI monitor and WMI daemon
# pip install wmi       — required for Tier 0 kernel-bridge process hook

# ── Optional: JA3 C2 Fingerprinting ──────────────────────────
# Requires Npcap first: https://npcap.com/
scapy>=2.5.0

# ── EMBER2024 ML Feature Extraction (thrember + signify) ──────
# thrember is NOT on PyPI. Install in this order:
#   pip install signify==0.7.1
#   git clone https://github.com/FutureComputing4AI/EMBER2024
#   cd EMBER2024 && pip install .
# Required for Tier 2 ML and SHAP. Cloud and AI tiers work without it.